name: ArtilleryIO Performance Test

env:
  PLAYWRIGHT_BROWSERS_PATH: /github/home/.cache/ms-playwright/chromium-1019/chrome-linux/chrome

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

jobs:
  load-test-satellite:
    runs-on: ubuntu-latest
    name: Create Account with Multiple VU's
    
    steps:
    - uses: actions/checkout@master

    - name: Install Playwright Browsers
      run: npx playwright install
    
    - name: Load Test Action Step 🔨
      id: load-test
      uses: luisecm/github-actions-artillery@master
      with:
        filepath: hello-satellite.yml

    #- name: Install artillery-playwright 🔨
    #  run: |
    #    npm install artillery-engine-playwright
    
    #- name: Load Test Action Step 🔨
    #  id: load-test-2
    #  uses: kenju/github-actions-artillery@master
    #  with:
    #    filepath: hello-satellite.yml
  